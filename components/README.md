# Components Directory

This directory contains reusable React components for the HaberNexus application.

## Structure (Planned for v1.0)

```
components/
├── ui/              # Basic UI components (buttons, inputs, cards)
├── layout/          # Layout components (header, footer, sidebar)
├── article/         # Article-related components
└── admin/           # Admin panel components
```

## Status

Currently empty. Components will be added during v1.0 MVP development.
